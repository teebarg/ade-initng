export const CookieName = Object.freeze({
  TOKEN: 'token',
});

const ApiEndPoint = Object.freeze({
  AUTH: '/login',
  BUSINESS: '/business',
  CATEGORY: '/category',
  IMAGE: '/image',
});

export default ApiEndPoint;
